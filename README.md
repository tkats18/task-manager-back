# task-manager-back

database -> PostgreSQL
java version -> 11

params:

    spring.datasource.username={username}
    spring.datasource.password={password}

configuration:

    Active Profiles: dev

DB :

    db: postgres
    schema: task_dev